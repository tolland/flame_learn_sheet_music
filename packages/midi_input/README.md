# Midi Input package

This packge is a facade to current the flutter_midi_command package
to coordinate receiving midi input from devices connected via bluetooth, usb or via the web midi api

Its currently a placeholder to move functinality to once its working